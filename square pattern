#include <stdio.h>

int main() {
  int i,j,rows=10,columns=10;
  for(i=1;i<=rows;i++)
  {
      for(j=1;j<=columns;j++)
      {
          printf("* ");
      }
      printf("\n");
  }
    return 0;
}
