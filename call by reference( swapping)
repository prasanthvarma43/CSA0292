#include<stdio.h>
void swap(int *a,int *b)
{
    int temp=*a;
    *a=*b;
    *b=temp;
}
int main()
{
    int x=50,y=100;
    printf("before swapping x=%d and y=%d\n",x,y);
    swap(&x,&y);
    printf("after swapping x=%d and y=%d",x,y);
    return 0;
}
